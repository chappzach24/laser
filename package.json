{
  "name": "laser",
  "version": "1.0.0",
  "description": "A MERN stack application for a store.",
  "main": "server/server.js",
  "scripts": {
    "start": "concurrently \"cd server && nodemon server.js\" \"cd client && npm run dev\"",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm run dev\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build",
    "seed": "cd server && npm run seed",
    "render-build": "npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "bootstrap": "^5.3.3",
    "concurrently": "^8.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^7.0.0"
  }
}
